<%= form_with(model: merch, local: true) do |form| %>
  <% if merch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(merch.errors.count, "error") %> prohibited this merch from being saved:</h2>

      <ul>
      <% merch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :merch_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :merch_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
